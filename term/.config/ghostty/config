theme = Nord
window-width = 120
window-height  = 40

# SSH: ssh-env sets environment compat, ssh-terminfo installs required terminfo on the remote host
shell-integration-features = ssh-env,ssh-terminfo
